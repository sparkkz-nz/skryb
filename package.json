{
  "name": "skryb-runtime",
  "private": true,
  "version": "0.0.0",
  "description": "Skryb portable HTML document runtime",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:docs": "node scripts/build-reference.mjs",
    "check": "tsc --noEmit",
    "lint": "node scripts/lint.mjs",
    "test": "npm run build && npm run build:docs && node --test test/render-runtime.test.js"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "5.9.3"
  }
}
